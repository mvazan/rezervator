import 'package:flutter/material.dart';

/// Temporary stub — replaced by the real shell in the week-grid task.
class HomeShell extends StatelessWidget {
  const HomeShell({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Rozvrh přijde vzápětí. 🎳')));
  }
}
