import 'package:flutter/material.dart';

/// Stub — day-override management arrives in Task 4.
class OverridesScreen extends StatelessWidget {
  const OverridesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Výjimky dnů')),
      body: const Center(child: Text('Zatím nic.')),
    );
  }
}
